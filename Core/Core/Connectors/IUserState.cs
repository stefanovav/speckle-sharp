namespace Speckle.Core.Connectors
{
  /// <summary>
  /// State for user related info.
  /// TBD: Not sure how we will handle user related state.
  ///   Maybe we will store them in Web? instead of Local AppData???
  /// </summary>
  public interface IUserState
  {
    /// TBD: We might collect here model specific preferences that user saved into file of the host app model.
  }
}
